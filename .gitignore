# Environment & Secrets
.env
scripts/.env
*.env
scripts/config.json

# Python
__pycache__/
*.pyc
*.pyo

# macOS
.DS_Store

# SQLite (retired — Supabase is now source of truth)
database/*.db

# Supabase backups — large JSON dumps excluded, CSVs tracked
backups/*_backup.json
supabase/.temp/
